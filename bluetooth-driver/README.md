# Bluetooth Host Driver (TODO: add submodule when upstream repo is ready)

This directory is reserved for the **Bluetooth Linux driver** Git submodule. The path name follows the conventions described in [docs/en/SUBMODULES.md](../docs/en/SUBMODULES.md) (English) / [docs/zh/SUBMODULES.md](../docs/zh/SUBMODULES.md) (中文).

Example command to add the submodule (run from the repository root; replace the URL with the actual upstream):

```bash
git submodule add <upstream-bluetooth-driver-repo-URL> bluetooth-driver
```

The applicable license is the `LICENSE` file in the root of the submodule repository.
